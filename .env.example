# ===================================
# Production Environment Variables
# ===================================

# DEPLOYMENT URL (Required for production)
# Set this to your actual domain when deploying
NEXTAUTH_URL="https://yourdomain.com"

# GENERATED KEYS (Generate new ones for production!)
# Run: openssl rand -base64 32
NEXTAUTH_SECRET="GENERATE_NEW_SECRET_FOR_PRODUCTION"
ENCRYPTION_KEY="GENERATE_NEW_KEY_FOR_PRODUCTION"
CRON_SECRET="GENERATE_NEW_SECRET_FOR_PRODUCTION"

# DEVELOPMENT MODE SETTINGS
DEV_BYPASS_PAYMENTS="true"  # Set to "true" to bypass payment processing in development

# DATABASE (from Neon.tech or your provider)
DATABASE_URL="postgresql://..."

# GOOGLE OAUTH (from Google Cloud Console)
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"
GOOGLE_AUTH_SCOPES="openid email profile https://www.googleapis.com/auth/gmail.modify"

# ANTHROPIC API (from console.anthropic.com)
ANTHROPIC_API_KEY="sk-ant-api03-..."

# STRIPE (from stripe.com/dashboard)
STRIPE_PUBLISHABLE_KEY="pk_test_your_publishable_key_here"
STRIPE_SECRET_KEY="sk_test_your_secret_key_here"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret_here"

# Create these products in Stripe Dashboard > Products
STRIPE_PRICE_ID_STARTER="price_starter_monthly_id"
STRIPE_PRICE_ID_PROFESSIONAL="price_professional_monthly_id"
STRIPE_PRICE_ID_BUSINESS="price_business_monthly_id"

# WEBHOOK SETUP:
# 1. Go to Stripe Dashboard > Webhooks > Add endpoint
# 2. URL: https://yourdomain.com/api/webhooks/stripe  
# 3. Events: checkout.session.completed, customer.subscription.created, customer.subscription.updated, customer.subscription.deleted, invoice.payment_failed
# 4. Copy the webhook signing secret to STRIPE_WEBHOOK_SECRET

# ===================================
# PRODUCTION CHECKLIST
# ===================================
# 1. Set NEXTAUTH_URL to your production domain
# 2. Generate new secrets for NEXTAUTH_SECRET, ENCRYPTION_KEY, CRON_SECRET
# 3. Update Google OAuth redirect URIs to include your domain
# 4. Create Lemon Squeezy webhook with your domain URL
# 5. Copy the webhook signing secret to LEMONSQUEEZY_WEBHOOK_SECRET
# 6. Ensure database is accessible from production environment
# 7. Test subscription flow with a real purchase